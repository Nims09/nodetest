- The api will serve the files needed to build the front end



---
- need to run mongod before running mongodb, try running with sudo if having issues


--- todo's 
- Make listens_import.bash take the listens.csv file location as a parameter
- need to get top 20 albums query within the fin